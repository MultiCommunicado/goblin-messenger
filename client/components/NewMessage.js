import React from 'react'

const NewMessage = props => {
    return (
        <div className="newMessage">
            We'll let the user input a new message here
        </div>
    );
}

export default NewMessage;