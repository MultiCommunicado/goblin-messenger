//const subject = require('../client/state/reducers/mainReducer.js');

describe('Reducer Tests', () => {
  let state;

  beforeEach(() => {
    state = {};
  });

  // - Default State
  describe('should return true', () => {
    it('should return true', () => {
      expect('true').toEqual('true');
    });
  });

  // - LOGIN_STATE
  describe('', () => {
    xit('should', () => {
      expect();
    });
  });

  // - LOGIN
  describe('', () => {
    xit('should', () => {
      expect();
    });
  });

  // - SIGNUP_STATE
  describe('', () => {
    xit('should', () => {
      expect();
    });
  });

  // - SIGNUP
  describe('', () => {
    xit('should', () => {
      expect();
    });
  });

  // - USER_INFO
  describe('', () => {
    xit('should', () => {
      expect();
    });
  });

  // - UPDATE_MESSAGES
  describe('', () => {
    xit('should', () => {
      expect();
    });
  });
});
